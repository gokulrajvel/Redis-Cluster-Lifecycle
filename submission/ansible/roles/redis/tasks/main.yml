---
- name: Create redis group
  group:
    name: "{{ redis_group }}"
    state: present
    system: yes

- name: Create redis user
  user:
    name: "{{ redis_user }}"
    group: "{{ redis_group }}"
    state: present
    system: yes
    create_home: no
    shell: /usr/sbin/nologin

- name: Create Redis directories
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ redis_user }}"
    group: "{{ redis_group }}"
    mode: '0755'
  loop:
    - "{{ redis_conf_dir }}"
    - "{{ redis_dir }}"
    - "{{ redis_log_dir }}"

- name: Download Redis source tarball
  get_url:
    url: "{{ redis_download_url }}"
    dest: "/tmp/redis-{{ redis_version }}.tar.gz"
    mode: '0644'

- name: Extract Redis source tarball
  unarchive:
    src: "/tmp/redis-{{ redis_version }}.tar.gz"
    dest: "/tmp"
    remote_src: yes

- name: Compile and install Redis from source
  make:
    chdir: "/tmp/redis-{{ redis_version }}"
    target: install
  become: yes

- name: Template Redis configuration file
  template:
    src: redis.conf.j2
    dest: "{{ redis_conf_dir }}/redis.conf"
    owner: "{{ redis_user }}"
    group: "{{ redis_group }}"
    mode: '0644'

- name: Create run directory
  file:
    path: /run/redis
    state: directory
    owner: "{{ redis_user }}"
    group: "{{ redis_group }}"
    mode: '0755'

- name: Check if Redis is already running
  command: pgrep -x redis-server
  register: redis_running
  ignore_errors: yes
  changed_when: false

- name: Start Redis manually
  shell: "redis-server {{ redis_conf_dir }}/redis.conf --daemonize yes"
  become: yes
  when: redis_running.rc != 0

- name: Wait for Redis to start
  wait_for:
    port: "{{ redis_port }}"
    host: "127.0.0.1"
    delay: 2
    timeout: 30